# Generated by `cap add ios` / `cap add android` — each dev runs these
# locally. We don't check the native projects in because they drag Xcode/
# Gradle toolchain lockfiles that update frequently and bloat the repo.
ios/
android/

# Capacitor transient state
.capacitor/
