app_identifier("app.cookyourbooks")
team_id("YNDYJ3A9CQ")

# Apple ID is read from FASTLANE_APPLE_ID env var when present (CI / scripts).
# Interactive runs will prompt and cache via Apple's 2FA flow.
apple_id(ENV["FASTLANE_APPLE_ID"]) if ENV["FASTLANE_APPLE_ID"]
