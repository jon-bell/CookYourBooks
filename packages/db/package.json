{
  "name": "@cookyourbooks/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gen-types": "../../.bin/supabase gen types typescript --local 2>/dev/null > src/database.types.ts"
  },
  "dependencies": {
    "@cookyourbooks/domain": "workspace:*",
    "@supabase/supabase-js": "^2.46.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
