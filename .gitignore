node_modules/
dist/
.turbo/
.next/
.vite/
coverage/
*.log
.DS_Store
*.swp
*.swo
*.tsbuildinfo

# Secrets + local config
.env
.env.local
.env.*.local
.env.prod
.secrets
supabase/.env.prod
supabase/.env.local

# Supabase CLI state
supabase/.branches/
supabase/.temp/

# Vendored Supabase CLI binary — ~100MB, platform-specific.
# Install via `scripts/install-supabase-cli.sh` (documented in CLAUDE.md).
.bin/

# Playwright artifacts
apps/web/test-results/
apps/web/playwright-report/

# Capacitor native projects: we commit ios/ (source-controlled bits only —
# Pods/, build/, the generated capacitor.config.json are excluded by the
# platform-level .gitignore that `cap add` wrote). android/ stays ignored
# until we scaffold + commit it the same way.
apps/mobile/android/
apps/mobile/.capacitor/

# Claude Code local state
.claude/
