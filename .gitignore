node_modules/
dist/
.turbo/
.next/
.vite/
coverage/
*.log
.DS_Store
*.swp
*.swo
*.tsbuildinfo

# Secrets + local config
.env
.env.local
.env.local.*
.env.*.local
.env.prod
.secrets
supabase/.env.prod
supabase/.env.local

# Supabase CLI state
supabase/.branches/
supabase/.temp/

# Vendored Supabase CLI binary — ~100MB, platform-specific.
# Install via `scripts/install-supabase-cli.sh` (documented in CLAUDE.md).
.bin/

# Playwright artifacts
apps/web/test-results/
apps/web/playwright-report/

# Capacitor native projects: we commit BOTH ios/ and android/ (source-
# controlled bits only — Pods/, build/, .gradle/, the generated
# capacitor.config.json are excluded by the platform-level .gitignore that
# `cap add` writes inside each dir, plus the keystore/secret rules in
# apps/mobile/.gitignore).
apps/mobile/.capacitor/

# Claude Code local state
.claude/
